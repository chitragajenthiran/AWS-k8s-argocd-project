# ===========================================
# POD DISRUPTION BUDGET (Production)
# ===========================================
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: gitops-demo
  labels:
    app: gitops-demo
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: gitops-demo

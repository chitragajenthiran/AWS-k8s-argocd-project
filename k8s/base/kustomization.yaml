# ===========================================
# KUSTOMIZATION - BASE
# ===========================================
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: gitops-demo-base

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - hpa.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/managed-by: argocd
